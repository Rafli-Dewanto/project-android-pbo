<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/colorPrimary</item>
    </style>

    <style name="buttonPrimary">
        <item name="android:background">@drawable/btn_selector</item>
    </style>

    <style name="buttonSecondary">
        <item name="android:background">@drawable/btn_outlined_selector</item>
    </style>

    <style name="buttonTertiary">
        <item name="android:background">@drawable/btn_gradient</item>
    </style>

    <style name="banner">
        <item name="background">@drawable/banner</item>
    </style>

    <style name="bannerError">
        <item name="background">@drawable/error_banner</item>
    </style>

</resources>
